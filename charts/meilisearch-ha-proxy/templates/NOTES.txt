MeiliSearch HA Proxy has been deployed!

  Release: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}

  Proxy endpoint:
    http://{{ include "meilisearch-ha-proxy.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

  MeiliSearch instances: {{ .Values.meilisearch.replicas }}
  Proxy replicas: {{ .Values.replicaCount }}

To verify the deployment:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

  # Port-forward to test locally
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "meilisearch-ha-proxy.fullname" . }} 7700:{{ .Values.service.port }}
  curl -s http://localhost:7700/health | jq
  curl -s http://localhost:7700/cluster/status | jq
